# spotify-friends-backend

## Setup

### required programms

- mongoDB

### configuration

windows (mac/linux: use `export` instead of `SET`)

```
Rename _config.js in config.js and set values in it
config.js is included in gitignore
```
